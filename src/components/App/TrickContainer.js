import React from 'react'
import Trick from './Trick'

const TrickContainer = () => {
    return (
//      Map through available tricks to display each of them
        )
}


export default TrickContainer